const { convertImageController } = require("./convertImage.controller");
const { downloadController } = require("./download.controller");

module.exports = {
  downloadController,
  convertImageController,
};
